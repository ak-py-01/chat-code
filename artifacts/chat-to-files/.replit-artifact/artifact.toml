kind = "web"
previewPath = "/"
title = "Chat to Files"
version = "1.0.0"
id = "artifacts/chat-to-files"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20588

[services.development]
run = "pnpm --filter @workspace/chat-to-files run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chat-to-files", "run", "build" ]
publicDir = "artifacts/chat-to-files/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20588"
BASE_PATH = "/"
